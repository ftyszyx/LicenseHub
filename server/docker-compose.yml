name: licensehub 
services:
  server:
    # build:
    #   context: ../server
    #   dockerfile: Dockerfile
    #   args:
    #     USE_ALIYUN_DEBIAN_MIRROR: 1
    #     USE_ALIYUN_CRATES_MIRROR: 1
    image: ftyszyx752/licensehub:latest
    env_file:
      - .env
    # depends_on:
    #   db:
    #     condition: service_started
    #   redis:
    #     condition: service_started 
    ports:
      - "${LISTEN_OUT_PORT}:${LISTEN_PORT}"
    volumes:
      - ./deploy/server/logs:/app/logs
    restart: unless-stopped
    networks:
      - 1panel-network
networks:
  1panel-network:
    external: true
  # db:
  #   image: postgres:17-alpine
  #   restart: unless-stopped
  #   environment:
  #     - POSTGRES_DB=${DB_NAME}
  #     - POSTGRES_USER=${DB_USER}
  #     - POSTGRES_PASSWORD=${DB_PASSWORD}
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - ./deploy/postgres/data:/var/lib/postgresql/data
  #     - ./deploy/postgres/init:/docker-entrypoint-initdb.d
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  # redis:
  #   image: redis:latest
  #   restart: unless-stopped
  #   ports:
  #     - "6379:6379"
